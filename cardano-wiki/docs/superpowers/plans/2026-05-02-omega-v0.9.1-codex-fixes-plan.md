# Omega v0.9.1 — Codex Audit Hardening Patch

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close the four findings from the Codex audit pass on `codex-review-2026-05-02` before v1.0 mainnet ingestion proceeds. Two P0 bugs (silent CLI no-output for 4 sub-trees; UTXO sub-tree drops native assets) plus two minor items (trailing-byte CBOR acceptance; doc/test-count drift).

**Architecture:** Five surgical patches against v0.9.0 — no architectural changes. Golden vectors WILL drift on the UTXO + token-policy + hybrid bundle paths because the UTXO leaf will now carry asset data; that is expected and correct. We re-pin all affected vectors as part of Task 4.

**Tech Stack:** Existing v0.9.0 dependencies, no additions.

**Track:** T1 (Ω-Commitment Tooling) — patch release between v0.9.0 and v1.0. After this lands, v1.0 plan can proceed.

**Source of findings:** `omega-commitment/docs/codex_findings/2026-05-02-omega-codex-review.md` (committed by Codex on branch `codex-review-2026-05-02`, commit `2464926`).

---

## Honest scope statement

These are real bugs. They didn't show up in `cargo test --workspace` because:
- The four broken CLI runners only exercise the path "argv parsed → input file read → exit 0"; they never assert on output content.
- The UTXO/asset gap was hidden by the v0.9.0 fixture writers explicitly populating `assets: Vec::new()` in v0.8.0 minimal-fixture testing, then never being audited against the v0.9.0 extended fixture for asset preservation.
- The trailing-byte issue is by-design CBOR self-delimitation — strict parsing is an extra check we should add but isn't a CBOR-spec violation.

The fixes are small. Test-count and golden-vector drift are mechanical to re-pin via the same fail-once-then-pin workflow used in v0.8.0/v0.9.0.

---

## File structure (post-plan)

```
omega-commitment/
├── crates/
│   └── omega-commitment-ingest/
│       ├── src/
│       │   ├── main.rs                                        (Task 1: fix 4 broken runners)
│       │   ├── utxo/synthetic.rs                              (Task 2: thread multi_assets into Utxo.assets)
│       │   ├── cbor.rs                                        (Task 3: strict trailing-byte detection helper)
│       │   ├── token_policy/synthetic.rs                      (Task 3: post-loop strict check)
│       │   ├── script/synthetic.rs                            (Task 3: post-loop strict check)
│       │   ├── stake/synthetic.rs                             (Task 3: post-loop strict check)
│       │   ├── governance/synthetic.rs                        (Task 3: post-loop strict check)
│       │   └── (utxo/synthetic.rs strict check folded into Task 2)
│       └── tests/
│           ├── golden_ingest.rs                               (Task 4: re-pin UTXO + bundle goldens)
│           └── (new test in cli.rs for CLI runner output presence)
├── crates/
│   └── omega-commitment-core/
│       └── tests/golden_vectors.rs                            (Task 4: NO change — synthetic-fixture roots unchanged)
└── crates/
    └── omega-commitment-bundle/
        └── tests/golden_bundle.rs                             (Task 4: NO change — synthetic-fixture bundle unchanged)
```

Note on which goldens drift:
- **`omega-commitment-core/tests/golden_vectors.rs`** — uses `omega-commitment-core/tests/fixtures/utxo_set_small.json` (which has `assets: [{...}]` populated already). Roots **DO NOT change**.
- **`omega-commitment-bundle/tests/golden_bundle.rs`** — uses the same JSON fixtures. Roots **DO NOT change**.
- **`omega-commitment-ingest/tests/golden_ingest.rs`** — UTXO ingestion from extended CBOR now produces `assets` populated (was empty). UTXO leaf hashes change → UTXO root changes → hybrid bundle root changes. Token-policy and script roots are derived from the same UTXO walk; their roots **DO NOT change** (they don't depend on whether assets surface in the UTXO sub-tree, only on whether multi-asset bundles are walked — which they already are). Net: **2 of the 7 pinned hexes in `golden_ingest.rs` change** (UTXO root + hybrid blake2b + hybrid sha3).

---

## Task 1: Fix the four broken CLI runners

**Files:**
- Modify: `crates/omega-commitment-ingest/src/main.rs`
- Modify: `crates/omega-commitment-ingest/tests/cli.rs` (add positive output-presence tests)

The v0.8.0 scaffold runners did `let _ = ingest_X(&cbor)?;` — discarding output, returning Ok. v0.9.0 replaced the `unimplemented!()` bodies with real implementations but the runners weren't updated. Fix: each runner mirrors `run_utxo`'s `fs::write(&output, serde_json::to_string_pretty(&out)?)?;` shape.

- [ ] **Step 1: Fix `run_token_policy`, `run_script`, `run_stake`, `run_governance` in `main.rs`**

Find each function. They currently look like:

```rust
fn run_token_policy(input: PathBuf, _output: PathBuf) -> Result<()> {
    let cbor = fs::read(&input)?;
    let _ = ingest_token_policies(&cbor)?;
    Ok(())
}
```

Replace each with the matching pattern from `run_utxo`. Final shape (showing token-policy; same skeleton for script/stake/governance):

```rust
fn run_token_policy(input: PathBuf, output: PathBuf) -> Result<()> {
    let cbor = fs::read(&input)?;
    let out = ingest_token_policies(&cbor)?;
    fs::write(&output, serde_json::to_string_pretty(&out)?)?;
    println!("ok: ingested {} policies -> {}", out.policies.len(), output.display());
    Ok(())
}

fn run_script(input: PathBuf, output: PathBuf) -> Result<()> {
    let cbor = fs::read(&input)?;
    let out = ingest_scripts(&cbor)?;
    fs::write(&output, serde_json::to_string_pretty(&out)?)?;
    println!("ok: ingested {} scripts -> {}", out.scripts.len(), output.display());
    Ok(())
}

fn run_stake(input: PathBuf, output: PathBuf) -> Result<()> {
    let cbor = fs::read(&input)?;
    let out = ingest_stake(&cbor)?;
    fs::write(&output, serde_json::to_string_pretty(&out)?)?;
    println!("ok: ingested {} stake_entries -> {}", out.stake_entries.len(), output.display());
    Ok(())
}

fn run_governance(input: PathBuf, output: PathBuf) -> Result<()> {
    let cbor = fs::read(&input)?;
    let out = ingest_governance(&cbor)?;
    fs::write(&output, serde_json::to_string_pretty(&out)?)?;
    println!("ok: ingested {} facts -> {}", out.facts.len(), output.display());
    Ok(())
}
```

Note the parameter name change from `_output: PathBuf` → `output: PathBuf` (drop the underscore prefix since it's now used).

- [ ] **Step 2: Add output-presence tests in `tests/cli.rs`**

Append four new smoke tests that ASSERT on the JSON output, not just exit code. After the existing `cli_commit_*_smoke` tests, add:

```rust
#[test]
fn cli_ingest_token_policy_writes_output() {
    let exe = env!("CARGO_BIN_EXE_omega-ingest");
    let fixture = fixture_path_extended();
    let out_dir = tempfile::tempdir().unwrap();
    let out_file = out_dir.path().join("policies.json");
    let status = Command::new(exe)
        .args(["token-policy", "--input", fixture.to_str().unwrap(), "--output", out_file.to_str().unwrap()])
        .status().expect("cli runs");
    assert!(status.success(), "exit code");
    assert!(out_file.exists(), "output file must exist");
    let body = fs::read_to_string(&out_file).unwrap();
    let parsed: serde_json::Value = serde_json::from_str(&body).unwrap();
    let policies = parsed["policies"].as_array().expect("policies array");
    assert_eq!(policies.len(), 2, "extended fixture has 2 policies");
}

#[test]
fn cli_ingest_script_writes_output() {
    let exe = env!("CARGO_BIN_EXE_omega-ingest");
    let fixture = fixture_path_extended();
    let out_dir = tempfile::tempdir().unwrap();
    let out_file = out_dir.path().join("scripts.json");
    let status = Command::new(exe)
        .args(["script", "--input", fixture.to_str().unwrap(), "--output", out_file.to_str().unwrap()])
        .status().expect("cli runs");
    assert!(status.success());
    assert!(out_file.exists());
    let body = fs::read_to_string(&out_file).unwrap();
    let parsed: serde_json::Value = serde_json::from_str(&body).unwrap();
    assert_eq!(parsed["scripts"].as_array().unwrap().len(), 2);
}

#[test]
fn cli_ingest_stake_writes_output() {
    let exe = env!("CARGO_BIN_EXE_omega-ingest");
    let fixture = fixture_path_stake();
    let out_dir = tempfile::tempdir().unwrap();
    let out_file = out_dir.path().join("stake.json");
    let status = Command::new(exe)
        .args(["stake", "--input", fixture.to_str().unwrap(), "--output", out_file.to_str().unwrap()])
        .status().expect("cli runs");
    assert!(status.success());
    assert!(out_file.exists());
    let body = fs::read_to_string(&out_file).unwrap();
    let parsed: serde_json::Value = serde_json::from_str(&body).unwrap();
    assert_eq!(parsed["stake_entries"].as_array().unwrap().len(), 4);
}

#[test]
fn cli_ingest_governance_writes_output() {
    let exe = env!("CARGO_BIN_EXE_omega-ingest");
    let fixture = fixture_path_governance();
    let out_dir = tempfile::tempdir().unwrap();
    let out_file = out_dir.path().join("gov.json");
    let status = Command::new(exe)
        .args(["governance", "--input", fixture.to_str().unwrap(), "--output", out_file.to_str().unwrap()])
        .status().expect("cli runs");
    assert!(status.success());
    assert!(out_file.exists());
    let body = fs::read_to_string(&out_file).unwrap();
    let parsed: serde_json::Value = serde_json::from_str(&body).unwrap();
    assert_eq!(parsed["facts"].as_array().unwrap().len(), 4);
}
```

If `fixture_path_extended` / `fixture_path_stake` / `fixture_path_governance` helpers don't exist yet, add them at the top of `cli.rs` modeled on the existing `fixture_path` helper:

```rust
fn fixture_path_extended() -> PathBuf {
    PathBuf::from(env!("CARGO_MANIFEST_DIR"))
        .join("tests/fixtures/ledger_state_extended.cbor")
}
fn fixture_path_stake() -> PathBuf {
    PathBuf::from(env!("CARGO_MANIFEST_DIR"))
        .join("tests/fixtures/stake_snapshot.cbor")
}
fn fixture_path_governance() -> PathBuf {
    PathBuf::from(env!("CARGO_MANIFEST_DIR"))
        .join("tests/fixtures/governance_snapshot.cbor")
}
```

- [ ] **Step 3: Verify the bug existed (before fix) then fix**

Before applying the fix, run the new tests against the still-broken code to confirm they catch the bug:

```bash
cd /home/hoskinson/omega-commitment
. "$HOME/.cargo/env"
cargo test -p omega-commitment-ingest --test cli cli_ingest_ 2>&1 | tail -20
```

Expected: 4 failures with "output file must exist" or "policies array" missing.

Then apply the runner fix and re-run:

```bash
cargo test -p omega-commitment-ingest --test cli 2>&1 | tail -10
```

Expected: all tests pass.

- [ ] **Step 4: Run full workspace + lint**

```bash
cargo test --workspace 2>&1 | tail -5
cargo lint 2>&1 | tail -3
cargo fmt-check 2>&1 | tail -3
```

- [ ] **Step 5: Commit**

```bash
git add crates/omega-commitment-ingest/src/main.rs crates/omega-commitment-ingest/tests/cli.rs
git -c user.email="charles.hoskinson@gmail.com" -c user.name="charles hoskinson" commit -m "fix(ingest-cli): four runners now write JSON output instead of discarding it (codex P0)"
```

---

## Task 2: Thread multi_assets into Utxo.assets

**Files:**
- Modify: `crates/omega-commitment-ingest/src/utxo/synthetic.rs`

Currently `ingest_utxos` walks the extended UTXO fixture's 6-elem entries via `arity == 6` branch and calls `skip_multi_assets(&mut d)?` — silently throwing away the multi-asset data. Fix: parse the multi-assets into `Vec<Asset>` and populate `Utxo.assets`.

The `Asset` type is in `omega_commitment_core::utxo_leaf::Asset` with shape `{ asset_id: Vec<u8>, quantity: u64 }`. The CBOR shape is `{ policy_id_28 => { asset_name_var => quantity_u64 } }`. Each `(policy_id, asset_name)` pair becomes one `Asset` with `asset_id = policy_id || asset_name` (concatenated bytes — matches the canonical Cardano native-asset identifier convention used elsewhere in the codebase).

Wait — confirm the encoding used by `omega-commitment-core`. The `Utxo::encode()` byte layout serializes assets as `(asset_id_len: u16 BE) || asset_id || (quantity: u64 BE)`. So `asset_id` is the full identifier bytes. Looking at existing leaf encoding: `Asset { asset_id: Vec<u8>, quantity: u64 }` — this matches the Cardano convention where `asset_id = policy_id (28 bytes) || asset_name (variable)`.

So the conversion: for each `(policy_id, asset_name) -> quantity` in the CBOR multi-asset map, produce one `Asset` with:
- `asset_id = [policy_id (28 bytes), asset_name bytes].concat()`
- `quantity = quantity_u64`

- [ ] **Step 1: Replace `skip_multi_assets` call with a parsing helper**

In `crates/omega-commitment-ingest/src/utxo/synthetic.rs`, find the function `skip_multi_assets`. Add a sibling function that PARSES into `Vec<Asset>`:

```rust
fn parse_multi_assets(d: &mut pallas_codec::minicbor::Decoder<'_>) -> anyhow::Result<Vec<omega_commitment_core::utxo_leaf::Asset>> {
    use omega_commitment_core::utxo_leaf::Asset;
    let mut assets = Vec::new();
    let n_policies = read_map_len(d)?;
    for _ in 0..n_policies {
        let policy: [u8; 28] = read_28_bytes(d)?;
        let n_assets = read_map_len(d)?;
        for _ in 0..n_assets {
            let name: Vec<u8> = read_var_bytes(d)?;
            let qty: u64 = read_u64(d)?;
            // asset_id = policy_id || asset_name (Cardano convention)
            let mut asset_id = Vec::with_capacity(28 + name.len());
            asset_id.extend_from_slice(&policy);
            asset_id.extend_from_slice(&name);
            assets.push(Asset { asset_id, quantity: qty });
        }
    }
    Ok(assets)
}
```

In the `ingest_utxos` for-loop body, find the `arity == 6` branch and change:

```rust
        if arity == 6 {
            skip_multi_assets(&mut d)?;
            skip_script_credential(&mut d)?;
        }
        utxos.push(Utxo {
            tx_id,
            output_index,
            address_hash,
            value_lovelace,
            assets: Vec::new(),       // <-- always empty currently
            datum_hash: None,
        });
```

To:

```rust
        let mut assets = Vec::new();
        if arity == 6 {
            assets = parse_multi_assets(&mut d)?;
            skip_script_credential(&mut d)?;
        }
        utxos.push(Utxo {
            tx_id,
            output_index,
            address_hash,
            value_lovelace,
            assets,
            datum_hash: None,
        });
```

The `skip_multi_assets` function is now dead code. Either delete it OR keep for the 4-elem path (which doesn't need it but it's still imported). Cleanest: delete `skip_multi_assets` entirely.

- [ ] **Step 2: Add a unit test confirming assets are preserved**

Append to `mod tests` in `utxo/synthetic.rs`:

```rust
#[test]
fn extended_fixture_preserves_multi_assets_in_utxos() {
    let cbor = std::fs::read("tests/fixtures/ledger_state_extended.cbor").unwrap();
    let out = ingest_utxos(&cbor).unwrap();
    assert_eq!(out.utxos.len(), 4);
    // UTXO 0: bare, no assets.
    assert!(out.utxos[0].assets.is_empty());
    // UTXO 1: one policy, one asset (COIN qty=100).
    assert_eq!(out.utxos[1].assets.len(), 1);
    assert_eq!(out.utxos[1].assets[0].quantity, 100);
    // UTXO 2: two policies, three assets total (COIN+NFT under policy_a, TOKEN under policy_b).
    assert_eq!(out.utxos[2].assets.len(), 3);
    let total_qty: u64 = out.utxos[2].assets.iter().map(|a| a.quantity).sum();
    assert_eq!(total_qty, 50 + 1 + 999);
    // UTXO 3: bare, no assets (script credential present but no multi-asset).
    assert!(out.utxos[3].assets.is_empty());
}
```

- [ ] **Step 3: Verify test fails before fix (if you didn't fix yet) then passes after**

```bash
cargo test -p omega-commitment-ingest utxo::synthetic::tests::extended_fixture_preserves_multi_assets_in_utxos 2>&1 | tail -10
```

If you fixed in Step 1 already, expect PASS. If you applied the test before the fix, it should fail with `assertion failed: out.utxos[1].assets.len() == 1` (currently 0).

- [ ] **Step 4: Verify integration tests still pass**

```bash
cargo test --workspace 2>&1 | tail -5
```

Note: `golden_ingest.rs::golden_utxo_root_from_extended_cbor` and the two hybrid bundle tests will FAIL because UTXO leaves now hash differently (assets included in encoding). That's expected — re-pin in Task 4. Token-policy + script roots are unchanged.

If you want to keep the lint clean during Task 2 commit, mark the affected golden tests `#[ignore = "v0.9.1: re-pin in Task 4"]` temporarily, then un-ignore in Task 4. Or commit Task 2 with the failures and chain Task 4's commit immediately.

- [ ] **Step 5: Commit**

```bash
git add crates/omega-commitment-ingest/src/utxo/synthetic.rs
git -c user.email="charles.hoskinson@gmail.com" -c user.name="charles hoskinson" commit -m "fix(ingest): UTXO sub-tree now preserves native assets per spec §9.1 (codex P0)"
```

---

## Task 3: Strict trailing-byte detection in CBOR ingestion

**Files:**
- Modify: `crates/omega-commitment-ingest/src/cbor.rs` (add helper)
- Modify: each of the 5 ingest paths in `crates/omega-commitment-ingest/src/{utxo,token_policy,script,stake,governance}/synthetic.rs`

After the main parse loop, assert that the decoder cursor is at end-of-input. If not, return an error.

- [ ] **Step 1: Add helper to `cbor.rs`**

Append to `crates/omega-commitment-ingest/src/cbor.rs`:

```rust
/// Assert the decoder cursor is at end-of-input. Returns Err if there
/// are trailing bytes beyond the parsed structure.
pub fn expect_end<'b>(d: &Decoder<'b>, total_len: usize) -> Result<()> {
    let pos = d.position();
    if pos != total_len {
        return Err(anyhow!(
            "cbor: trailing bytes after parse: position {} of {} (extra {} bytes)",
            pos, total_len, total_len - pos
        ));
    }
    Ok(())
}
```

Add a unit test at the bottom of cbor.rs `mod tests`:

```rust
#[test]
fn expect_end_succeeds_on_exact_consume() {
    let buf = vec![0x05]; // a single u64
    let mut d = Decoder::new(&buf);
    let _ = read_u64(&mut d).unwrap();
    expect_end(&d, buf.len()).unwrap();
}

#[test]
fn expect_end_fails_on_trailing_garbage() {
    let buf = vec![0x05, 0xFF, 0xAB]; // u64 + 2 trailing bytes
    let mut d = Decoder::new(&buf);
    let _ = read_u64(&mut d).unwrap();
    assert!(expect_end(&d, buf.len()).is_err());
}
```

- [ ] **Step 2: Wire the strict check into each ingest path**

For each of `utxo/synthetic.rs`, `token_policy/synthetic.rs`, `script/synthetic.rs`, `stake/synthetic.rs`, `governance/synthetic.rs`:

1. Add `expect_end` to the `use crate::cbor::{...}` import block.
2. After the main `for _ in 0..n` loop, before `Ok(...)`, add:

```rust
    expect_end(&d, cbor.len())?;
```

The `cbor: &[u8]` parameter is already in scope as the function input; `d: Decoder` is the local mut decoder.

Add a unit test in each file's `mod tests`:

```rust
#[test]
fn ingest_rejects_trailing_garbage() {
    let cbor_buf = std::fs::read("tests/fixtures/<the_fixture>.cbor").unwrap();
    let mut tampered = cbor_buf.clone();
    tampered.push(0xFF); // trailing byte
    let result = ingest_<funcname>(&tampered);
    assert!(result.is_err(), "trailing byte must be rejected");
}
```

(Substitute the appropriate fixture filename and ingest function name per file. UTXO uses `ledger_state_extended.cbor`; token-policy/script use the same; stake uses `stake_snapshot.cbor`; governance uses `governance_snapshot.cbor`.)

- [ ] **Step 3: Verify**

```bash
cargo test -p omega-commitment-ingest 2>&1 | tail -10    # all pass including 5 new strict tests
cargo test --workspace 2>&1 | tail -5
cargo lint 2>&1 | tail -3
cargo fmt-check 2>&1 | tail -3
```

- [ ] **Step 4: Commit**

```bash
git add crates/omega-commitment-ingest/src/cbor.rs \
        crates/omega-commitment-ingest/src/utxo/synthetic.rs \
        crates/omega-commitment-ingest/src/token_policy/synthetic.rs \
        crates/omega-commitment-ingest/src/script/synthetic.rs \
        crates/omega-commitment-ingest/src/stake/synthetic.rs \
        crates/omega-commitment-ingest/src/governance/synthetic.rs
git -c user.email="charles.hoskinson@gmail.com" -c user.name="charles hoskinson" commit -m "feat(ingest): strict trailing-byte detection in all five CBOR parsers (codex medium)"
```

---

## Task 4: Re-pin affected golden vectors

**Files:**
- Modify: `crates/omega-commitment-ingest/tests/golden_ingest.rs`

UTXO leaves now hash differently (assets included). Affected pinned hex strings:
- `golden_utxo_root_from_extended_cbor` — drift expected
- `golden_hybrid_bundle_roots` — both blake2b + sha3 drift

Token-policy and script ingest unchanged. Stake and governance unchanged.

Use the same fail-once-then-pin workflow as v0.8.0/v0.9.0:

- [ ] **Step 1: Replace the affected pinned hexes with placeholders**

Open `crates/omega-commitment-ingest/tests/golden_ingest.rs`. For:
- `golden_utxo_root_from_extended_cbor` — change the expected hex to `"deadbeef"`
- `golden_hybrid_bundle_roots` — change BOTH expected hexes (blake2b + sha3) to `"deadbeef"`

Leave token-policy / script / stake / governance pins untouched.

- [ ] **Step 2: Run the tests; capture the actual hexes from failure messages**

```bash
cargo test -p omega-commitment-ingest --test golden_ingest 2>&1 | tail -40
```

Each modified test will fail with `assertion failed: 'left == right'`. Read the actual hex from the "left" side.

- [ ] **Step 3: Replace placeholders with the new actual hexes**

Edit `golden_ingest.rs`. Replace the three `"deadbeef"` strings with the three captured hex values.

- [ ] **Step 4: Re-run; confirm passing**

```bash
cargo test -p omega-commitment-ingest --test golden_ingest 2>&1 | tail -10
cargo test --workspace 2>&1 | tail -5
cargo lint 2>&1 | tail -3
cargo fmt-check 2>&1 | tail -3
```

- [ ] **Step 5: Commit**

```bash
git add crates/omega-commitment-ingest/tests/golden_ingest.rs
git -c user.email="charles.hoskinson@gmail.com" -c user.name="charles hoskinson" commit -m "test(qa): re-pin v0.9.1 golden roots after UTXO asset-preservation fix"
```

---

## Task 5: Documentation + test-count corrections + version bump

**Files:**
- Modify: `crates/omega-commitment-core/Cargo.toml` (version bump to 0.9.1)
- Modify: `crates/omega-commitment-cli/Cargo.toml`
- Modify: `crates/omega-commitment-bundle/Cargo.toml`
- Modify: `crates/omega-commitment-ingest/Cargo.toml`
- Modify: `README.md`
- Modify: `cardano-wiki/wiki/log.md`

Bump the workspace to v0.9.1, document the patch, correct the test-count drift Codex flagged.

- [ ] **Step 1: Bump versions**

In each of the four `Cargo.toml` files: `version = "0.9.0"` → `version = "0.9.1"`.

- [ ] **Step 2: Get the correct test count**

```bash
cargo test --workspace 2>&1 | tail -10
```

Note the exact count (likely ~234 = 228 prior + ~6 net new from Tasks 1+3 + adjustments from Task 2).

- [ ] **Step 3: Append v0.9.1 release notes to README.md**

```markdown
## v0.9.1 — Codex audit fixes (P0 + medium + doc-drift)

Patch release closing four findings from the Codex audit pass on
`codex-review-2026-05-02` (commit `2464926`):

- **P0 (CLI):** Four `omega-ingest` runners (`token-policy`, `script`,
  `stake`, `governance`) discarded their library output and exited
  successfully. Now they write JSON to `--output` like `utxo` does.
  Four new `cli_ingest_*_writes_output` tests assert on file contents.
- **P0 (semantics):** UTXO sub-tree ingestion no longer drops native
  asset bundles. The 6-elem extended fixture's `multi_assets` field
  is now parsed into `Utxo.assets` (was silently set to `Vec::new()`),
  matching spec §9.1's `claim_utxo` "resurrects ADA + native tokens"
  contract. Token-policy and script sub-trees still derive from the
  same multi-asset walk.
- **Medium (CBOR):** All five CBOR parsers now reject trailing bytes
  via a new `cbor::expect_end` helper. Previously, garbage suffix on
  a valid CBOR prefix was silently ignored.
- **Low (docs):** Brief and wiki log claimed 236 tests; actual count
  was 228 in v0.9.0. Corrected to <new actual> in v0.9.1.

### Golden vectors that drifted

UTXO sub-tree leaf encoding now includes asset bundles, so:
- `omega-commitment-ingest/tests/golden_ingest.rs::golden_utxo_root_from_extended_cbor` — re-pinned
- `omega-commitment-ingest/tests/golden_ingest.rs::golden_hybrid_bundle_roots` — both blake2b + sha3 re-pinned

Synthetic per-sub-tree golden vectors in `omega-commitment-core` and
the bundle golden vector in `omega-commitment-bundle` are UNCHANGED
(they consume JSON fixtures that already had `assets` populated).
```

- [ ] **Step 4: Update wiki log**

Edit `/home/hoskinson/cardano-wiki/wiki/log.md`. Append after the v0.9.0 entry:

```markdown
## [2026-05-02] codex-audit | omega-commitment review on branch codex-review-2026-05-02
- 4 findings on commit 2464926 in docs/codex_findings/2026-05-02-omega-codex-review.md
- P0: 4 ingest CLI runners discard output and exit Ok (token-policy/script/stake/governance)
- P0: UTXO sub-tree drops native assets (skip_multi_assets) — conflicts with spec §9.1 claim_utxo
- Medium: CBOR parsers accept trailing bytes silently
- Low: brief claims 236 tests; cargo runs 228
- Verdict: do NOT proceed to v1.0 mainnet ingestion until P0s fixed; v0.9.1 patch follows

## [2026-05-02] plan + execute | omega-commitment v0.9.1 (Codex audit fixes)
- Plan: docs/superpowers/plans/2026-05-02-omega-v0.9.1-codex-fixes-plan.md (5 tasks)
- All four crates bumped to 0.9.1
- Fix #1: 4 broken ingest CLI runners now write JSON output (+ 4 new output-presence tests)
- Fix #2: UTXO sub-tree preserves native assets via parse_multi_assets (+ unit test)
- Fix #3: cbor::expect_end strict trailing-byte detection wired into all 5 parsers (+ 5 new tests)
- Fix #4: 3 ingestion-layer golden vectors re-pinned (UTXO root, hybrid blake2b, hybrid sha3 — all changed because UTXO leaves now include asset bundles)
- Doc fix: corrected test count from 236 to <actual>
- v1.0 (real-mainnet ingestion) NOW UNBLOCKED
```

- [ ] **Step 5: Verify everything green**

```bash
cd /home/hoskinson/omega-commitment
. "$HOME/.cargo/env"
cargo build --workspace 2>&1 | grep -E "warning|error"   # empty
cargo lint 2>&1 | tail -3                                  # clean
cargo fmt-check 2>&1 | tail -3                             # clean
cargo test --workspace 2>&1 | tail -5                       # all pass; record exact count
```

- [ ] **Step 6: Commit**

```bash
git add crates/omega-commitment-core/Cargo.toml \
        crates/omega-commitment-cli/Cargo.toml \
        crates/omega-commitment-bundle/Cargo.toml \
        crates/omega-commitment-ingest/Cargo.toml \
        README.md
git -c user.email="charles.hoskinson@gmail.com" -c user.name="charles hoskinson" commit -m "chore: bump workspace to 0.9.1; document codex audit fixes"
```

(The wiki log update commits separately into the cardano-wiki repo — different working tree.)

---

## Self-review

**Coverage of Codex findings:**
- ✅ P0 #1 (4 broken CLI runners) — Task 1
- ✅ P0 #2 (UTXO drops native assets) — Task 2
- ✅ Medium (trailing bytes) — Task 3
- ✅ Low (test count drift) — Task 5 Step 4

**Decision honoring (unchanged):**
- ✅ PQ-only crypto, Plonky3-friendly tree, selective dual-track at bundle layer, lazy/pull migration.
- ✅ Spec §9.1 (`claim_utxo` resurrects ADA + native tokens) — Task 2 brings UTXO in line.

**Placeholder scan:** All code blocks are concrete. The 3 hex placeholders in Task 4 use the documented "fail-once-then-pin" workflow (same pattern as v0.8.0/v0.9.0).

**Type consistency:**
- `Asset { asset_id: Vec<u8>, quantity: u64 }` — uses the existing `omega_commitment_core::utxo_leaf::Asset` type; no new type invented.
- `expect_end(&d, cbor.len())` signature consistent across all 5 ingest paths.
- Runner functions follow `run_utxo`'s established shape exactly.

**Bite-sized tasks:** 5 tasks, each independently committable.

**Net delta:** ~6 tests (4 CLI output-presence + 2 cbor::expect_end + 5 ingest trailing-byte + 1 utxo asset-preservation = 12 net new; some replace existing). Final count ~234.

---

## What's NOT in this plan (and why)

- **Full mainnet re-validation** — the patch is against synthetic fixtures only. Mainnet ingestion happens in v1.0; the patch unblocks but doesn't perform that.
- **Updating the Codex briefing's pinned-vector list** — separate maintenance pass on `cardano-wiki/docs/codex_briefings/2026-05-01-omega-codex-debug-brief.md`. Optional follow-up.
- **Mithril certificate verification in download script** — pre-existing low-priority item, unrelated to Codex findings.

---

## How to execute this plan

Use `superpowers:subagent-driven-development` (recommended). Five tasks, each independently committable. Total runway: **~3-5 hours** (small, surgical patches).

Order matters: Tasks 1, 2, 3 are independent and can run in any order, BUT Task 2 will break the golden_ingest tests. Suggested order: 1 → 3 → 2 → 4 → 5. Or: do all 1+2+3 implementations, accept golden_ingest failures, then do Task 4 to re-pin in one shot, then Task 5.

After execution, v1.0 (real-mainnet ingestion via Daedalus 8.0 or headless cardano-node) is unblocked.
