# Omega Commitment v0.3.x Hardening Sprint Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Drain the 7-item hardening backlog (plus 1 doc-clarity carry-over) accumulated across v0.1.0–v0.3.0 reviews before sub-tree 4 lands. Ship as v0.3.1.

**Architecture:** Eight independent tasks, each fixing one operational hygiene item. No new features, no breaking API changes (path traversal guard is the only behavior change, and only rejects paths that escape the output dir — a footgun, not a feature). Bump to v0.3.1 at the end.

**Tech Stack:** Rust 1.79+ workspace already in place. Adds no runtime deps. Adds `tempfile` (already a dev-dep) usage in non-test code for atomic writes.

**Track:** T1 (Ω-Commitment Tooling), hardening interlude between sub-tree 3 and sub-tree 4.

**Locked design decisions honored (unchanged by this sprint):**
- PQ-only crypto (Decision 7).
- Plonky3-friendly tree layout (Decision 8).
- Lazy/pull migration (Decision 2).
- Dual-track shadow hash deferred (program-level pending).

---

## Backlog mapping (review-trace)

| # | Carry-over item | Source review | Task |
|---|---|---|---|
| 1 | Path traversal on --input / --output | v0.1.0 final + v0.2.0 final | Task 1 |
| 2 | Input file size cap | v0.1.0 final + v0.2.0 final | Task 2 |
| 3 | Atomic write of commitment.json | v0.1.0 final M-5 | Task 3 |
| 4 | Layer cloning in tree.rs::build | v0.1.0 final I-4 + v0.2.0 final | Task 4 |
| 5 | Hex codec deduplication | v0.1.0 final M-3 + v0.2.0 final | Task 5 |
| 6 | Clippy + rustfmt CI | v0.1.0 final M-7 + v0.2.0 final | Task 6 |
| 7 | CLI dispatcher refactor | v0.2.0 final | Task 7 |
| 8 | leaf_count vs item_count doc clarity | v0.2.0 final | Task 8 |
| 9 | Bump to v0.3.1 + README | n/a — release task | Task 9 |

---

## File structure (post-plan)

```
omega-commitment/
├── Cargo.toml                                        (workspace, unchanged)
├── README.md                                         (extended with v0.3.1 notes)
├── rust-toolchain.toml                               (NEW — stable + clippy + rustfmt)
├── .github/workflows/ci.yml                          (NEW — build + test + clippy + fmt)
├── crates/
│   ├── omega-commitment-core/
│   │   ├── Cargo.toml                                (version bump to 0.3.1)
│   │   ├── src/
│   │   │   ├── lib.rs                                (add `pub mod serde_helpers`)
│   │   │   ├── hash.rs                               (unchanged)
│   │   │   ├── serde_helpers.rs                      (NEW — hex_vec_hash + opt_hex consolidated)
│   │   │   ├── utxo_leaf.rs                          (modify: use serde_helpers::opt_hex; drop local mod)
│   │   │   ├── header_leaf.rs                        (unchanged)
│   │   │   ├── tx_index_leaf.rs                      (unchanged)
│   │   │   ├── tree.rs                               (modify: drop one Vec clone in build)
│   │   │   └── witness.rs                            (modify: use serde_helpers::hex_vec_hash; drop local mod)
│   │   └── tests/                                    (unchanged structure)
│   └── omega-commitment-cli/
│       ├── Cargo.toml                                (version bump to 0.3.1; tempfile moved to non-dev dep)
│       ├── src/main.rs                               (refactor: dispatcher + path guard + size cap + atomic write + doc comments)
│       └── tests/cli.rs                              (extend with new safety-test cases)
```

Each file has one clear responsibility. The new `serde_helpers.rs` is the *only* place serde adapter logic lives.

---

## Task 1: Path traversal guard on `--input` / `--output`

**Files:**
- Modify: `crates/omega-commitment-cli/src/main.rs`
- Modify: `crates/omega-commitment-cli/tests/cli.rs`

The CLI today writes `witness_dir.join(<hex>.json)` and `output.join("commitment.json")`. `hex::encode` only emits `[0-9a-f]` so the current call is safe by construction — but the safety is implicit. As sub-tree types proliferate, future filenames may incorporate user data. Add an explicit invariant: every write path must be within the canonicalized output dir.

- [ ] **Step 1: Write the failing test**

Append to `/home/hoskinson/omega-commitment/crates/omega-commitment-cli/tests/cli.rs`:

```rust
#[test]
fn cli_rejects_input_path_that_does_not_exist() {
    let exe = env!("CARGO_BIN_EXE_omega-commitment");
    let out = tempfile::tempdir().unwrap();
    let status = Command::new(exe)
        .args([
            "commit",
            "--sub-tree", "utxo",
            "--input", "/nonexistent/path/utxos.json",
            "--output", out.path().to_str().unwrap(),
        ])
        .status()
        .expect("cli runs");
    assert!(!status.success(), "missing input must fail");
}
```

- [ ] **Step 2: Run test to verify it currently fails by accident or wrong-error**

```bash
cd /home/hoskinson/omega-commitment
. "$HOME/.cargo/env"
cargo test -p omega-commitment-cli --test cli cli_rejects_input_path_that_does_not_exist 2>&1 | tail -10
```

Expected: today already fails with a generic IO error. Confirm a non-zero exit. The test should already pass because `fs::read_to_string` returns Err on missing files. If so, treat this as a regression-pin for the new logic in Step 3.

- [ ] **Step 3: Add path canonicalization to `commit()`**

Edit `crates/omega-commitment-cli/src/main.rs`. At the top of `commit()`, immediately after the function signature line, replace the body's first lines (which currently start with `let raw = fs::read_to_string(&input)?;`) with:

```rust
fn commit(input: PathBuf, output: PathBuf, sub_tree: SubTree) -> anyhow::Result<()> {
    // Canonicalize the input (must exist) for clearer errors and to anchor
    // any future relative-path semantics.
    let input = input.canonicalize()
        .map_err(|e| anyhow::anyhow!("cannot read input {}: {}", input.display(), e))?;
    // Output dir must be either pre-existing or creatable; canonicalize after
    // create_dir_all to anchor the safe-write boundary.
    fs::create_dir_all(&output)
        .map_err(|e| anyhow::anyhow!("cannot create output dir {}: {}", output.display(), e))?;
    let output = output.canonicalize()
        .map_err(|e| anyhow::anyhow!("cannot resolve output {}: {}", output.display(), e))?;

    let raw = fs::read_to_string(&input)?;
    let input_digest = blake2b_256(raw.as_bytes());
    // ...rest of function unchanged below...
```

Then delete the existing `fs::create_dir_all(&output)?;` line later in the function (it now lives at the top).

Add a small helper above `commit()` that future writes can use:

```rust
/// Ensure a file path stays within `base_dir`. Returns the canonicalized
/// child path on success.
fn safe_child(base_dir: &std::path::Path, child: &std::path::Path) -> anyhow::Result<PathBuf> {
    // We cannot canonicalize a path that doesn't yet exist, so we resolve
    // the parent (which must exist after create_dir_all) and join the file
    // name back on. Then we string-prefix-check against the base.
    let parent = child.parent()
        .ok_or_else(|| anyhow::anyhow!("write path has no parent: {}", child.display()))?;
    let parent = parent.canonicalize()
        .map_err(|e| anyhow::anyhow!("cannot resolve write parent {}: {}", parent.display(), e))?;
    let file_name = child.file_name()
        .ok_or_else(|| anyhow::anyhow!("write path has no filename: {}", child.display()))?;
    let resolved = parent.join(file_name);
    if !resolved.starts_with(base_dir) {
        anyhow::bail!("write path escapes output dir: {} not in {}",
            resolved.display(), base_dir.display());
    }
    Ok(resolved)
}
```

In the witness-loop, where the code currently writes `witness_dir.join(fname)`, change it to:

```rust
        let target = safe_child(&output, &witness_dir.join(&fname))?;
        fs::write(&target, serde_json::to_string_pretty(&w)?)?;
```

Keep `commitment.json` written via `output.join("commitment.json")` unmodified — that filename is hard-coded by the binary, not user-derived.

- [ ] **Step 4: Run all CLI tests**

```bash
cargo test -p omega-commitment-cli --test cli 2>&1 | tail -15
```

Expected: all 4 prior tests + the new `cli_rejects_input_path_that_does_not_exist` pass (5 total).

- [ ] **Step 5: Commit**

```bash
git add crates/omega-commitment-cli/src/main.rs crates/omega-commitment-cli/tests/cli.rs
git -c user.email="charles.hoskinson@gmail.com" -c user.name="charles hoskinson" commit -m "feat(cli): canonicalize paths + safe_child write guard"
```

---

## Task 2: Input file size cap

**Files:**
- Modify: `crates/omega-commitment-cli/src/main.rs`
- Modify: `crates/omega-commitment-cli/tests/cli.rs`

Add `--max-input-bytes <BYTES>` to the `commit` subcommand, defaulting to 2 GiB (`2_147_483_648`). Reject inputs whose size exceeds the cap *before* allocating a `String` for them.

- [ ] **Step 1: Write the failing test**

Append to `crates/omega-commitment-cli/tests/cli.rs`:

```rust
#[test]
fn cli_rejects_oversized_input() {
    let exe = env!("CARGO_BIN_EXE_omega-commitment");
    let out = tempfile::tempdir().unwrap();
    let status = Command::new(exe)
        .args([
            "commit",
            "--sub-tree", "utxo",
            "--input", fixture_path("utxo_set_small.json").to_str().unwrap(),
            "--output", out.path().to_str().unwrap(),
            "--max-input-bytes", "10",
        ])
        .status()
        .expect("cli runs");
    assert!(!status.success(), "10-byte cap must reject the fixture");
}
```

- [ ] **Step 2: Run test to verify it fails**

```bash
cargo test -p omega-commitment-cli --test cli cli_rejects_oversized_input 2>&1 | tail -10
```

Expected: FAIL — flag does not yet exist.

- [ ] **Step 3: Add the flag and check**

In `crates/omega-commitment-cli/src/main.rs`, modify the `Cmd::Commit` variant. The current arg block is:

```rust
    Commit {
        #[arg(short, long)]
        input: PathBuf,
        #[arg(short, long)]
        output: PathBuf,
        #[arg(short = 's', long, value_enum, default_value_t = SubTree::Utxo)]
        sub_tree: SubTree,
    },
```

Change it to:

```rust
    Commit {
        #[arg(short, long)]
        input: PathBuf,
        #[arg(short, long)]
        output: PathBuf,
        #[arg(short = 's', long, value_enum, default_value_t = SubTree::Utxo)]
        sub_tree: SubTree,
        /// Maximum input file size in bytes. Default 2 GiB.
        #[arg(long, default_value_t = 2_147_483_648u64)]
        max_input_bytes: u64,
    },
```

Update the dispatcher in `main()`:

```rust
fn main() -> anyhow::Result<()> {
    let cli = Cli::parse();
    match cli.cmd {
        Cmd::Commit { input, output, sub_tree, max_input_bytes } =>
            commit(input, output, sub_tree, max_input_bytes),
    }
}
```

Update `commit()` signature and add the size check. Insert immediately after the `input.canonicalize()` lines (before `fs::read_to_string`):

```rust
fn commit(
    input: PathBuf,
    output: PathBuf,
    sub_tree: SubTree,
    max_input_bytes: u64,
) -> anyhow::Result<()> {
    let input = input.canonicalize()
        .map_err(|e| anyhow::anyhow!("cannot read input {}: {}", input.display(), e))?;
    let metadata = fs::metadata(&input)
        .map_err(|e| anyhow::anyhow!("cannot stat input {}: {}", input.display(), e))?;
    if metadata.len() > max_input_bytes {
        anyhow::bail!(
            "input file {} is {} bytes, exceeds --max-input-bytes={}",
            input.display(), metadata.len(), max_input_bytes);
    }
    fs::create_dir_all(&output)
        .map_err(|e| anyhow::anyhow!("cannot create output dir {}: {}", output.display(), e))?;
    let output = output.canonicalize()
        .map_err(|e| anyhow::anyhow!("cannot resolve output {}: {}", output.display(), e))?;

    let raw = fs::read_to_string(&input)?;
    // ...rest unchanged...
```

- [ ] **Step 4: Run the new test**

```bash
cargo test -p omega-commitment-cli --test cli cli_rejects_oversized_input 2>&1 | tail -10
```

Expected: PASS.

- [ ] **Step 5: Run all CLI tests**

```bash
cargo test -p omega-commitment-cli --test cli 2>&1 | tail -10
```

Expected: 6 tests pass (4 prior + Task 1's + this one).

- [ ] **Step 6: Commit**

```bash
git add crates/omega-commitment-cli/src/main.rs crates/omega-commitment-cli/tests/cli.rs
git -c user.email="charles.hoskinson@gmail.com" -c user.name="charles hoskinson" commit -m "feat(cli): --max-input-bytes flag with 2 GiB default"
```

---

## Task 3: Atomic write of `commitment.json`

**Files:**
- Modify: `crates/omega-commitment-cli/Cargo.toml`
- Modify: `crates/omega-commitment-cli/src/main.rs`

`tempfile` was a dev-dep. Promote to a regular dep so we can use `NamedTempFile::persist()` for an atomic write of `commitment.json`. (Witness files stay non-atomic — they're per-leaf and a half-written witness is recoverable by re-running.)

- [ ] **Step 1: Move `tempfile` to runtime deps**

Edit `/home/hoskinson/omega-commitment/crates/omega-commitment-cli/Cargo.toml`. Move `tempfile = "3"` from `[dev-dependencies]` to `[dependencies]`:

```toml
[dependencies]
omega-commitment-core = { path = "../omega-commitment-core" }
clap.workspace = true
serde.workspace = true
serde_json.workspace = true
hex.workspace = true
thiserror.workspace = true
anyhow = "1"
tempfile = "3"

[dev-dependencies]
```

(Keep the `[dev-dependencies]` section header; it can be empty or hold other dev-only deps.)

- [ ] **Step 2: Use `NamedTempFile::persist()` to atomically write `commitment.json`**

In `crates/omega-commitment-cli/src/main.rs`, find the line:

```rust
    fs::write(
        output.join("commitment.json"),
        serde_json::to_string_pretty(&record)?,
    )?;
```

Replace with:

```rust
    {
        // Write to a temp file in the output dir, then atomic-rename.
        let mut tmp = tempfile::Builder::new()
            .prefix(".commitment.")
            .suffix(".json.tmp")
            .tempfile_in(&output)?;
        use std::io::Write;
        tmp.write_all(serde_json::to_string_pretty(&record)?.as_bytes())?;
        tmp.flush()?;
        tmp.persist(output.join("commitment.json"))?;
    }
```

- [ ] **Step 3: Write a test that the file ends up at the expected path**

The existing `cli_commit_utxo_smoke` already asserts `commitment.json` exists after a run, so it implicitly tests the atomic-rename path. Add a more focused test:

```rust
#[test]
fn cli_commitment_file_lands_at_expected_path() {
    let out = run_commit("utxo", "utxo_set_small.json");
    assert!(out.path().join("commitment.json").exists());
    // No leftover .tmp files.
    let leftovers: Vec<_> = fs::read_dir(out.path()).unwrap()
        .filter_map(|r| r.ok())
        .filter(|e| {
            let n = e.file_name();
            let s = n.to_string_lossy();
            s.starts_with(".commitment.") && s.ends_with(".json.tmp")
        })
        .collect();
    assert!(leftovers.is_empty(), "tempfile not cleaned up: {leftovers:?}");
}
```

Append this to `crates/omega-commitment-cli/tests/cli.rs`.

- [ ] **Step 4: Verify**

```bash
cargo build --workspace 2>&1 | grep -E "warning|error"   # empty
cargo test -p omega-commitment-cli --test cli 2>&1 | tail -10   # 7 tests pass
```

- [ ] **Step 5: Commit**

```bash
git add crates/omega-commitment-cli/Cargo.toml crates/omega-commitment-cli/src/main.rs crates/omega-commitment-cli/tests/cli.rs
git -c user.email="charles.hoskinson@gmail.com" -c user.name="charles hoskinson" commit -m "feat(cli): atomic write of commitment.json via tempfile + rename"
```

---

## Task 4: Drop a layer-clone in `tree.rs::build`

**Files:**
- Modify: `crates/omega-commitment-core/src/tree.rs`

Current build does two clones per non-leaf layer. Replace one of them. Don't over-rewrite — this is a targeted perf fix, not a rewrite.

- [ ] **Step 1: Add a benchmark sanity assertion test**

This isn't a failing test in the TDD sense; it's a regression pin for tree behavior. Add to `crates/omega-commitment-core/src/tree.rs` at the bottom of `mod tests`:

```rust
    #[test]
    fn build_preserves_root_after_perf_change() {
        // Pin the root for a known input so any future change to build()
        // that produces a different root will fail loudly.
        let leaves: Vec<Hash> = (0..16u8).map(|i| blake2b_256(&[i])).collect();
        let t = MerkleTree::build(leaves);
        // Recorded with v0.3.1 implementation. If you change build()
        // and this fails, you may have changed the canonical root —
        // that is a SemVer break and requires program-level review.
        assert_eq!(t.depth(), 4);
        assert_eq!(t.leaf_count(), 16);
        // Root is whatever build() produces; this test documents it.
        // To regenerate after a verified change: print and copy.
        let _ = t.root();
    }
```

Note: this test does NOT pin the actual hex bytes of the root because we are about to refactor the building code and the root *must* remain identical — but locking the bytes here would create a chicken-and-egg with the refactor commit. The depth/leaf-count assertions catch any structural change, and the existing integration tests catch any root-bytes drift.

- [ ] **Step 2: Run all tree tests**

```bash
cd /home/hoskinson/omega-commitment
. "$HOME/.cargo/env"
cargo test -p omega-commitment-core tree::tests 2>&1 | tail -10
```

Expected: all tree tests pass (8 prior + 1 new = 9).

- [ ] **Step 3: Refactor `build()` to drop one clone**

In `crates/omega-commitment-core/src/tree.rs`, find the existing `build` function. The current loop is:

```rust
        let mut layers = vec![input.clone()];
        let mut current = input;
        while current.len() > 1 {
            let mut next = Vec::with_capacity(current.len() / 2);
            for chunk in current.chunks(2) {
                let mut buf = [0u8; 64];
                buf[..32].copy_from_slice(&chunk[0]);
                buf[32..].copy_from_slice(&chunk[1]);
                next.push(blake2b_256(&buf));
            }
            layers.push(next.clone());
            current = next;
        }
        Self { leaves: layers[0].clone(), layers }
```

Replace the loop body with the version below. We move `input` into `layers` once (no clone) and read each previous layer back via `layers.last()`:

```rust
        let mut layers: Vec<Vec<Hash>> = vec![input];
        while layers.last().expect("non-empty").len() > 1 {
            let prev = layers.last().expect("non-empty");
            let mut next = Vec::with_capacity(prev.len() / 2);
            for chunk in prev.chunks(2) {
                let mut buf = [0u8; 64];
                buf[..32].copy_from_slice(&chunk[0]);
                buf[32..].copy_from_slice(&chunk[1]);
                next.push(blake2b_256(&buf));
            }
            layers.push(next);
        }
        Self { leaves: layers[0].clone(), layers }
```

We still clone `layers[0]` once into the dedicated `leaves` field for ergonomic access via `tree.leaves()`. That's a single one-shot clone, not a per-layer clone.

- [ ] **Step 4: Run all tests**

```bash
cargo test --workspace 2>&1 | tail -5
```

Expected: 59 tests pass (58 prior + Task 1 new + Task 2 new + Task 3 new + Task 4 new — but several were CLI-side, so the core delta is +1 for `build_preserves_root_after_perf_change` here). The integration tests across all 3 sub-trees confirm the root bytes are unchanged.

- [ ] **Step 5: Re-run criterion benchmark to confirm perf is the same or better**

```bash
cargo bench -p omega-commitment-core 2>&1 | tail -30
```

Expected: 100k UTXO build is no slower than before (we removed two clones, never added any). Don't gate on a specific speedup; just verify it didn't regress.

- [ ] **Step 6: Commit**

```bash
git add crates/omega-commitment-core/src/tree.rs
git -c user.email="charles.hoskinson@gmail.com" -c user.name="charles hoskinson" commit -m "perf(tree): drop redundant layer clones in build()"
```

---

## Task 5: Consolidate hex serde adapters into `serde_helpers.rs`

**Files:**
- Create: `crates/omega-commitment-core/src/serde_helpers.rs`
- Modify: `crates/omega-commitment-core/src/lib.rs`
- Modify: `crates/omega-commitment-core/src/utxo_leaf.rs`
- Modify: `crates/omega-commitment-core/src/witness.rs`

Today `witness.rs` has a private `mod hex_vec_hash` and `utxo_leaf.rs` has a private `mod opt_hex`. Pull both into `serde_helpers.rs`, then have the consumers use the shared form.

- [ ] **Step 1: Create the new module**

Create `/home/hoskinson/omega-commitment/crates/omega-commitment-core/src/serde_helpers.rs`:

```rust
//! Reusable serde adapters for hash-typed fields.
//!
//! - `hex_vec_hash`: serialize/deserialize `Vec<Hash>` as JSON array of
//!   lowercase hex strings.
//! - `opt_hex`: serialize/deserialize `Option<[u8; 32]>` as a hex string
//!   or `null`.

use crate::hash::Hash;
use serde::{Deserialize, Deserializer, Serialize, Serializer};

/// Serde adapter for `Vec<Hash>` -> JSON array of hex strings.
///
/// Use as `#[serde(with = "crate::serde_helpers::hex_vec_hash")]`.
pub mod hex_vec_hash {
    use super::*;

    pub fn serialize<S: Serializer>(v: &Vec<Hash>, s: S) -> Result<S::Ok, S::Error> {
        let strs: Vec<String> = v.iter().map(hex::encode).collect();
        strs.serialize(s)
    }

    pub fn deserialize<'de, D: Deserializer<'de>>(d: D) -> Result<Vec<Hash>, D::Error> {
        let strs: Vec<String> = Vec::deserialize(d)?;
        strs.iter()
            .map(|s| {
                let bytes = hex::decode(s).map_err(serde::de::Error::custom)?;
                if bytes.len() != 32 {
                    return Err(serde::de::Error::custom("hash must be 32 bytes"));
                }
                let mut arr = [0u8; 32];
                arr.copy_from_slice(&bytes);
                Ok(arr)
            })
            .collect()
    }
}

/// Serde adapter for `Option<[u8; 32]>` -> hex string or null.
///
/// Use as `#[serde(with = "crate::serde_helpers::opt_hex", default)]`.
pub mod opt_hex {
    use super::*;

    pub fn serialize<S: Serializer>(
        v: &Option<[u8; 32]>,
        s: S,
    ) -> Result<S::Ok, S::Error> {
        match v {
            None => s.serialize_none(),
            Some(bytes) => s.serialize_some(&hex::encode(bytes)),
        }
    }

    pub fn deserialize<'de, D: Deserializer<'de>>(
        d: D,
    ) -> Result<Option<[u8; 32]>, D::Error> {
        let opt: Option<String> = Option::deserialize(d)?;
        match opt {
            None => Ok(None),
            Some(s) => {
                let bytes = hex::decode(&s).map_err(serde::de::Error::custom)?;
                if bytes.len() != 32 {
                    return Err(serde::de::Error::custom("hash must be 32 bytes"));
                }
                let mut arr = [0u8; 32];
                arr.copy_from_slice(&bytes);
                Ok(Some(arr))
            }
        }
    }
}

#[cfg(test)]
mod tests {
    use super::*;
    use serde::{Deserialize, Serialize};

    #[derive(Serialize, Deserialize, PartialEq, Debug)]
    struct VecWrapper(#[serde(with = "hex_vec_hash")] Vec<Hash>);

    #[derive(Serialize, Deserialize, PartialEq, Debug)]
    struct OptWrapper {
        #[serde(with = "opt_hex", default)]
        h: Option<[u8; 32]>,
    }

    #[test]
    fn hex_vec_hash_roundtrip() {
        let original = VecWrapper(vec![[0x11u8; 32], [0x22u8; 32]]);
        let s = serde_json::to_string(&original).unwrap();
        assert!(s.contains("\"1111111111"));
        let parsed: VecWrapper = serde_json::from_str(&s).unwrap();
        assert_eq!(parsed, original);
    }

    #[test]
    fn opt_hex_some_roundtrip() {
        let original = OptWrapper { h: Some([0xAAu8; 32]) };
        let s = serde_json::to_string(&original).unwrap();
        assert!(s.contains("\"aaaaaaaa"));
        let parsed: OptWrapper = serde_json::from_str(&s).unwrap();
        assert_eq!(parsed, original);
    }

    #[test]
    fn opt_hex_none_roundtrip() {
        let original = OptWrapper { h: None };
        let s = serde_json::to_string(&original).unwrap();
        assert!(s.contains("null"));
        let parsed: OptWrapper = serde_json::from_str(&s).unwrap();
        assert_eq!(parsed, original);
    }

    #[test]
    fn opt_hex_default_when_field_missing() {
        // {} with `default` should produce h = None.
        let parsed: OptWrapper = serde_json::from_str("{}").unwrap();
        assert_eq!(parsed, OptWrapper { h: None });
    }

    #[test]
    fn hex_vec_hash_rejects_wrong_length() {
        let bad = "[\"abcd\"]";
        let r: Result<VecWrapper, _> = serde_json::from_str(bad);
        assert!(r.is_err(), "should reject 2-byte hex as Hash");
    }

    #[test]
    fn opt_hex_rejects_wrong_length() {
        let bad = "{\"h\":\"abcd\"}";
        let r: Result<OptWrapper, _> = serde_json::from_str(bad);
        assert!(r.is_err(), "should reject 2-byte hex as Hash");
    }
}
```

- [ ] **Step 2: Expose the module in `lib.rs`**

Replace `crates/omega-commitment-core/src/lib.rs` contents with:

```rust
//! omega-commitment-core: Ω-Commitment sub-tree library.
//!
//! Provides canonical leaf encodings, a Plonky3-friendly Merkle tree, and
//! inclusion witnesses. v0.3.x supports three of seven Ω-Commitment sub-trees:
//! UTXO set, block header chain, and transaction index.

pub mod hash;
pub mod serde_helpers;
pub mod tree;
pub mod witness;
pub mod utxo_leaf;
pub mod header_leaf;
pub mod tx_index_leaf;
```

- [ ] **Step 3: Update `utxo_leaf.rs` to use the shared `opt_hex`**

In `crates/omega-commitment-core/src/utxo_leaf.rs`:
- Delete the local `mod opt_hex { ... }` block (and any `use` it required that is now unused).
- Change the `#[serde(with = "opt_hex", default)]` annotation on `datum_hash` to `#[serde(with = "crate::serde_helpers::opt_hex", default)]`.

If the file imports `serde::{Deserialize, Deserializer, Serialize, Serializer}` for `opt_hex`'s use only, those imports can stay; they're harmless. Don't aggressively prune.

- [ ] **Step 4: Update `witness.rs` to use the shared `hex_vec_hash`**

In `crates/omega-commitment-core/src/witness.rs`:
- Delete the local `mod hex_vec_hash { ... }` block.
- Change `#[serde(with = "hex_vec_hash")]` on `siblings` to `#[serde(with = "crate::serde_helpers::hex_vec_hash")]`.

- [ ] **Step 5: Run tests**

```bash
cargo build --workspace 2>&1 | grep -E "warning|error"   # empty
cargo test --workspace 2>&1 | tail -5
```

Expected: 65 tests pass total — was 59 after Task 4, +6 from `serde_helpers::tests`. Existing utxo_leaf and witness tests still pass because the shared adapter behaves identically.

- [ ] **Step 6: Commit**

```bash
git add crates/omega-commitment-core/src/serde_helpers.rs \
        crates/omega-commitment-core/src/lib.rs \
        crates/omega-commitment-core/src/utxo_leaf.rs \
        crates/omega-commitment-core/src/witness.rs
git -c user.email="charles.hoskinson@gmail.com" -c user.name="charles hoskinson" commit -m "refactor: consolidate hex serde adapters into serde_helpers module"
```

---

## Task 6: Clippy + rustfmt CI

**Files:**
- Create: `rust-toolchain.toml`
- Create: `.cargo/config.toml`
- Create: `.github/workflows/ci.yml`

Pin the toolchain to stable + clippy + rustfmt; add a CI workflow that runs build + test + clippy + fmt-check on every push.

- [ ] **Step 1: Pin the toolchain**

Create `/home/hoskinson/omega-commitment/rust-toolchain.toml`:

```toml
[toolchain]
channel = "stable"
components = ["clippy", "rustfmt"]
profile = "minimal"
```

- [ ] **Step 2: Add cargo defaults**

Create `/home/hoskinson/omega-commitment/.cargo/config.toml`:

```toml
[alias]
fmt-check = "fmt --all -- --check"
lint = "clippy --workspace --all-targets -- -D warnings"
```

- [ ] **Step 3: Add CI workflow**

Create `/home/hoskinson/omega-commitment/.github/workflows/ci.yml`:

```yaml
name: ci

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
      - name: Toolchain
        run: |
          rustup show
      - name: fmt-check
        run: cargo fmt-check
      - name: clippy
        run: cargo lint
      - name: test
        run: cargo test --workspace
```

- [ ] **Step 4: Install clippy and rustfmt locally and run them**

```bash
cd /home/hoskinson/omega-commitment
. "$HOME/.cargo/env"
rustup component add clippy rustfmt 2>&1 | tail -3
cargo fmt-check 2>&1 | tail -20
cargo lint 2>&1 | tail -40
```

If `cargo fmt-check` shows diff, run `cargo fmt --all` and re-run check.

If `cargo lint` reports warnings, fix them inline. The `-D warnings` flag means any warning fails the build. Common fixes:
- Replace `if !x.is_empty() { ... }` with `if !x.is_empty() { ... }` (already idiomatic — this is just an example).
- Replace `let mut x = Vec::new(); x.push(y);` with `let x = vec![y];`.
- For genuinely-needed `#[allow(...)]` annotations, scope them as narrowly as possible.

If clippy flags something genuinely controversial in our code (e.g., `clippy::needless_range_loop` on a loop where the index is meaningful), prefer a one-line `#[allow(clippy::needless_range_loop)]` with a brief comment over rewriting the loop.

- [ ] **Step 5: Re-run all tests**

```bash
cargo fmt-check 2>&1 | tail -3   # no diff
cargo lint 2>&1 | tail -5         # no warnings
cargo test --workspace 2>&1 | tail -5   # 65 tests pass
```

- [ ] **Step 6: Commit**

```bash
git add rust-toolchain.toml .cargo/config.toml .github/workflows/ci.yml
# Plus any source files you had to fix to clear clippy/fmt:
# git add <files modified to satisfy clippy/fmt>
git -c user.email="charles.hoskinson@gmail.com" -c user.name="charles hoskinson" commit -m "ci: pin toolchain + clippy/fmt workflow"
```

---

## Task 7: Decompose CLI dispatcher

**Files:**
- Modify: `crates/omega-commitment-cli/src/main.rs`

The current `commit()` is ~80 lines and the giant `match sub_tree { ... }` is 22 of them. Pull each arm's leaf-hash construction into its own free function so adding sub-tree 4 doesn't grow the dispatcher much. Keep it simple — no traits, no registry. This is YAGNI-aligned.

- [ ] **Step 1: Extract three free functions**

In `crates/omega-commitment-cli/src/main.rs`, add three module-private functions just *before* `fn commit(`:

```rust
fn build_utxo_leaves(raw: &str) -> anyhow::Result<(Vec<Hash>, usize)> {
    let parsed: UtxoInput = serde_json::from_str(raw)?;
    let leaves: Vec<Hash> = parsed.utxos.iter()
        .map(|u| u.leaf_hash())
        .collect::<Result<Vec<_>, _>>()?;
    let n = parsed.utxos.len();
    Ok((leaves, n))
}

fn build_header_leaves(raw: &str) -> anyhow::Result<(Vec<Hash>, usize)> {
    let parsed: HeaderInput = serde_json::from_str(raw)?;
    let leaves: Vec<Hash> = parsed.headers.iter().map(|h| h.leaf_hash()).collect();
    let n = parsed.headers.len();
    Ok((leaves, n))
}

fn build_tx_index_leaves(raw: &str) -> anyhow::Result<(Vec<Hash>, usize)> {
    let parsed: TxIndexInput = serde_json::from_str(raw)?;
    let leaves: Vec<Hash> = parsed.entries.iter().map(|e| e.leaf_hash()).collect();
    let n = parsed.entries.len();
    Ok((leaves, n))
}
```

- [ ] **Step 2: Replace the giant match in `commit()` with a 3-line dispatch**

Find the existing `match sub_tree { ... }` block in `commit()` and replace with:

```rust
    let (leaves, item_count) = match sub_tree {
        SubTree::Utxo => build_utxo_leaves(&raw)?,
        SubTree::Header => build_header_leaves(&raw)?,
        SubTree::TxIndex => build_tx_index_leaves(&raw)?,
    };
```

- [ ] **Step 3: Run all tests**

```bash
cargo build --workspace 2>&1 | grep -E "warning|error"   # empty
cargo lint 2>&1 | tail -5                                  # no warnings
cargo test --workspace 2>&1 | tail -5                       # 65 tests pass
```

- [ ] **Step 4: Commit**

```bash
git add crates/omega-commitment-cli/src/main.rs
git -c user.email="charles.hoskinson@gmail.com" -c user.name="charles hoskinson" commit -m "refactor(cli): extract per-sub-tree leaf builders out of commit()"
```

---

## Task 8: `CommitmentRecord` field doc clarity

**Files:**
- Modify: `crates/omega-commitment-cli/src/main.rs`

The reviewer noted that `leaf_count` (post-padding) and `item_count` (pre-padding) coexist without explicit docs and the distinction is easy to miss in the JSON output. Add doc comments.

- [ ] **Step 1: Add field doc comments**

In `crates/omega-commitment-cli/src/main.rs`, find the `CommitmentRecord` struct and replace it with:

```rust
#[derive(Serialize)]
struct CommitmentRecord {
    /// Which Ω-Commitment sub-tree this record describes.
    sub_tree: SubTree,
    /// Blake2b-256 of the raw input file bytes. Lets consumers confirm
    /// the commitment is bound to a specific input snapshot.
    #[serde(with = "hex::serde")]
    input_digest: Hash,
    /// The Merkle root of this sub-tree.
    #[serde(with = "hex::serde")]
    root: Hash,
    /// Number of leaves in the tree AFTER padding to the next power of two.
    /// Always >= `item_count`. Equal when `item_count` is itself a power
    /// of two.
    leaf_count: usize,
    /// Depth of the tree, where depth 0 means a single-leaf tree (root == leaf).
    tree_depth: usize,
    /// Number of items in the input BEFORE padding. The semantic unit
    /// depends on `sub_tree`: UTXOs, headers, or tx-index entries.
    item_count: usize,
}
```

- [ ] **Step 2: Run tests**

```bash
cargo lint 2>&1 | tail -5
cargo test --workspace 2>&1 | tail -5
```

Expected: 65 tests pass; no lint or fmt issues.

- [ ] **Step 3: Commit**

```bash
git add crates/omega-commitment-cli/src/main.rs
git -c user.email="charles.hoskinson@gmail.com" -c user.name="charles hoskinson" commit -m "docs(cli): clarify CommitmentRecord field semantics"
```

---

## Task 9: Bump to v0.3.1 + update README

**Files:**
- Modify: `crates/omega-commitment-core/Cargo.toml`
- Modify: `crates/omega-commitment-cli/Cargo.toml`
- Modify: `README.md`

- [ ] **Step 1: Bump versions**

Edit `crates/omega-commitment-core/Cargo.toml`: `version = "0.3.0"` -> `version = "0.3.1"`.

Edit `crates/omega-commitment-cli/Cargo.toml`: `version = "0.3.0"` -> `version = "0.3.1"`.

- [ ] **Step 2: Verify build and tests**

```bash
cargo build --workspace 2>&1 | grep -E "warning|error"   # empty
cargo lint 2>&1 | tail -5                                  # no warnings
cargo test --workspace 2>&1 | tail -5                       # 65 tests pass
```

- [ ] **Step 3: Append release notes to `README.md`**

Append at the end of `/home/hoskinson/omega-commitment/README.md`:

```markdown
## v0.3.1 — Hardening sprint (no new sub-trees)

Closes the v0.1.0/v0.2.0 hardening backlog before sub-tree 4 lands. No new functionality, no breaking API changes (one minor behavior change: see "Path safety" below).

### Operational hygiene

- **Path safety:** CLI now canonicalizes `--input` (must exist) and `--output` (creatable). Witness files are guarded by a `safe_child` helper that refuses to write outside the canonicalized output dir. Today this is defense-in-depth (filenames are hex-only); it preempts a class of bugs as future sub-tree filenames could include richer data.
- **Input file size cap:** new `--max-input-bytes <BYTES>` flag, default 2 GiB. Prevents OOM on accidental large inputs.
- **Atomic write:** `commitment.json` is now written to a temp file and atomically renamed into place. A crash mid-run leaves no half-written commitment.

### Code quality

- **Hex serde adapters consolidated** into `omega_commitment_core::serde_helpers` (`hex_vec_hash` for `Vec<Hash>`, `opt_hex` for `Option<[u8; 32]>`). `utxo_leaf` and `witness` now use the shared module.
- **CLI dispatcher decomposed** into per-sub-tree free functions (`build_utxo_leaves`, `build_header_leaves`, `build_tx_index_leaves`). `commit()` is now ~30 lines.
- **`CommitmentRecord` field docs** clarify `leaf_count` (post-padding) vs. `item_count` (pre-padding).

### Performance

- **One redundant Vec clone per layer removed** from `MerkleTree::build`. No behavior change; smoke-tested via existing 256-leaf and integration tests, plus a new pinned-shape regression test.

### CI

- **`rust-toolchain.toml`** pins stable + clippy + rustfmt.
- **`.cargo/config.toml`** adds `cargo fmt-check` and `cargo lint` aliases.
- **`.github/workflows/ci.yml`** runs build + test + clippy (`-D warnings`) + fmt-check on every push and PR.

### Carried forward (still open)

- **Dual-track shadow hash** — program-level pending decision. Plonky3 circuit authors must NOT lock to v0.3.x single-root format.

### Sub-trees status (unchanged)

| # | Sub-tree | Plan | Status |
|---|---|---|---|
| 1 | UTXO set | `2026-05-01-omega-utxo-commitment-plan.md` | Shipped (v0.1.0) |
| 2 | Block header chain | `2026-05-01-omega-block-header-accumulator-plan.md` | Shipped (v0.2.0) |
| 3 | Transaction index | `2026-05-01-omega-tx-index-plan.md` | Shipped (v0.3.0) |
| 4 | Native token policies | TBD | Pending |
| 5 | Script registry | TBD | Pending |
| 6 | Stake state | TBD | Pending |
| 7 | Governance state | TBD | Pending |

The next plan is the program-level **dual-track shadow hash decision**, then sub-tree 4 (token policies).
```

- [ ] **Step 4: Commit**

```bash
git add crates/omega-commitment-core/Cargo.toml \
        crates/omega-commitment-cli/Cargo.toml \
        README.md
git -c user.email="charles.hoskinson@gmail.com" -c user.name="charles hoskinson" commit -m "chore: bump to 0.3.1; document hardening sprint"
```

- [ ] **Step 5: Final verification**

```bash
git log --oneline | head -12
cargo test --workspace 2>&1 | tail -5
cargo lint 2>&1 | tail -3
cargo fmt-check 2>&1 | tail -3
```

Expected: 9 new commits since v0.3.0, 65 tests pass, no lint or fmt warnings.

---

## Self-review

**Backlog coverage:**
- ✅ Path traversal — Task 1
- ✅ Input size cap — Task 2
- ✅ Atomic write — Task 3
- ✅ Layer cloning — Task 4
- ✅ Hex codec deduplication — Task 5
- ✅ Clippy + rustfmt CI — Task 6
- ✅ CLI dispatcher refactor — Task 7
- ✅ Field doc clarity — Task 8
- ✅ Version bump + README — Task 9

All items from the v0.1.0 and v0.2.0 final-review backlogs are addressed except the program-level dual-hash decision (correctly out-of-scope).

**Decision adherence:**
- ✅ PQ-only crypto — unchanged.
- ✅ Plonky3-friendly tree — `tree.rs` perf change does NOT alter root values; pinned by integration tests + new regression test.
- ✅ Lazy/pull migration — unchanged.
- ✅ Dual-hash deferred — explicitly noted in README as still open.

**No placeholders:** Every code block is complete; no "TBD", "fill in", or "similar to". ✅

**Type consistency:**
- `safe_child(&Path, &Path) -> anyhow::Result<PathBuf>` referenced consistently in Task 1.
- `--max-input-bytes` flag spelled identically in Task 2 test, code, and README.
- `serde_helpers::hex_vec_hash` and `serde_helpers::opt_hex` paths are consistent across Task 5 (definition) and the consumers (utxo_leaf, witness).
- `build_utxo_leaves` / `build_header_leaves` / `build_tx_index_leaves` named consistently in Task 7 definition and dispatcher.
- ✅ No drift.

**Bite-sized tasks:** 9 tasks, each independently committable; each step is one action. ✅

---

## What's NOT in this plan (and why)

- **Sub-tree 4 (token policies).** Out of scope by design; this plan is hardening only.
- **Dual-track shadow hash decision.** Program-level, not engineering. Needs a separate decision document, not a code patch.
- **Streaming JSON parser.** A 2 GiB string allocation is OK for v0.3.1; streaming is a sub-tree-4-or-later concern.
- **Repository URL fix (M-6 from v0.1.0).** This depends on whether the IntersectMBO org actually creates the repo — non-engineering decision. Update later when known.
- **Mainnet snapshot regression test.** Needs `cardano-multiplatform-lib`; deferred per the same logic as v0.1.0/v0.2.0.

---

## How to execute this plan

Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans`. Nine tasks, each independently committable.

Total runway estimate: **3–5 days** for an experienced Rust dev. Most tasks are surgical; Task 6 (CI) and Task 5 (consolidation) are the largest.

Expected post-execution state:
- ~9 commits added on top of v0.3.0 (currently 29 commits)
- ~7 net new tests (58 → 65)
- Both crates at version 0.3.1
- `cargo fmt-check` and `cargo lint` clean
- Sub-tree 4 (token policies) plan slot is unblocked

After this sprint, the natural next step is to surface the **dual-track shadow hash decision** at program level before sub-tree 4 starts — the README explicitly calls this out as the gating item.
