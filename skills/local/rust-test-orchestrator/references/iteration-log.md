# Iteration log

One-line entries per skill iteration. Format:

```
YYYY-MM-DD <skill-name>: <failure mode> → <SKILL.md change that fixed it>
```

## Entries

(empty — populate as T1, T2, T3 reveal issues)
