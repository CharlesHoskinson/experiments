# Fixture F1 — leaf_hash_v2

End-to-end golden-vector test for the orchestrator. Target is `omega_commitment_core::leaf_hash_v2`.

## Trigger prompt

> "Write tests for `omega_commitment_core::leaf_hash_v2` — I want to cover correctness, fuzzing, and soundness-negative cases."

## Expected Phase 1 ground

```
target file: omega-commitment/crates/omega-commitment-core/src/lib.rs (or wherever leaf_hash_v2 lives)
Cargo.toml (workspace): blake3=yes, sha3=yes, proptest=yes, no kani-verifier (will be added)
Cargo.toml (crate):     blake3=yes
grep target file:       pub fn leaf_hash_v2 takes &[u8]; no async, no tokio, no Arc<Mutex>, no openraft
```

## Expected Phase 2 matrix

```
target: omega_commitment_core::leaf_hash_v2
ground: blake3=yes, no async, no Arc<Mutex>, takes &[u8] payload, pub fn
matrix:
  Q1 network/IO?           → no    → skip turmoil, madsim, failpoints
  Q2 shared mut state?     → no    → skip shuttle-loom
  Q3 untrusted bytes?      → yes   → invoke rust-test-cargo-fuzz
  Q4 bounded invariant?    → yes   → invoke rust-test-kani  (bound: payload_len ≤ 4KB)
  Q5 random-input prop?    → yes   → invoke rust-test-proptest
  Q6 protocol w/ N parties? → no   → skip stateright
soundness-negative cases planned: 3
  - leaf-as-internal-node second-preimage swap (must reject)
  - EMPTY_INDEX_SENTINEL forgery at idx >= item_count (must reject)
  - leaf with idx >= u32::MAX (must reject as out-of-bounds)
```

## Expected Phase 3 outcome

User approves. Orchestrator proceeds to Phase 4.

## Expected Phase 4 invocations (in order)

1. `rust-test-kani` — writes 1 harness for collision-resistance under bounded inputs
2. `rust-test-proptest` — writes 8 property tests covering round-trip, monotonicity, distinctness
3. `rust-test-cargo-fuzz` — writes 1 fuzz target for `leaf_hash_v2` with seed corpus from `tests/golden_vectors/per_leaf/`

## Expected Phase 5 report

```
# Test run report — omega_commitment_core::leaf_hash_v2 @ <sha>

## Results
| Skill | Tests written | Passed | Failed | Time | Notes |
|---|---|---|---|---|---|
| kani         | 1 harness   | 1/1 | 0/1 | <60s | bound: payload_len ≤ 4KB |
| proptest     | 8 cases     | 8/8 | 0/8 | <30s | 4096 inputs/case |
| cargo-fuzz   | 1 target    | n/a | n/a | 600s | 0 crashes |

## Soundness-negative tests (Adversary class)
| Case | Expected | Observed | Status |
|---|---|---|---|
| leaf-as-internal-node second-preimage swap | reject | reject | ✅ |
| EMPTY_INDEX_SENTINEL forgery at idx >= item_count | reject | reject | ✅ |
| leaf with idx >= u32::MAX | reject | reject | ✅ |

## P0 alerts
None.

STATUS: GREEN
```

## Pass criteria

F1 passes if (a) the matrix matches exactly, (b) the right skills were invoked in the right order, (c) the report's STATUS is `GREEN`. F1 must pass before the pack ships v0.1.0.

## How to run

1. Open a fresh Claude Code session in `c:\experiments\`.
2. Paste the trigger prompt above.
3. Compare each phase's output to the expected.
4. Record any deviation in `iteration-log.md`.
